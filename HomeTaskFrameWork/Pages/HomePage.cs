using OpenQA.Selenium;


namespace HomeTaskFrameWork.Pages
{
    internal class HomePage : BasePage
    {
        private readonly IWebDriver _webDriver;

        internal HomePage(IWebDriver webDriver) : base(webDriver)
        {
            _webDriver = webDriver;
        }

        /* Locators */
       
        /* Elements */
        
        /* Methods */        
    }
}